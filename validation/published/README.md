# Published validation bundles

This folder is intended for *curated*, git-friendly validation artifacts for releases.

- Runs happen under `validation/runs/<run-id>/` (transient).
- Selected artifacts get exported here via `validation/scripts/export_run_bundle.sh`.

See `validation/PUBLISHING.md` for the convention.
