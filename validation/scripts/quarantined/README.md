# Quarantined Validation Scripts

This directory contains legacy validation helpers that are not part of the v1.1
release validation surface.

- `create_corrected_spectra.py` was removed from the active tree. It used
  personal absolute paths and was not maintained with the current validation
  runners.
